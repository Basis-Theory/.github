{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Basis Theory org-wide Renovate config preset",
  "extends": [
    "config:best-practices",
    "helpers:pinGitHubActionDigestsToSemver"
  ],
  "timezone": "UTC",
  "schedule": ["before 6am on monday"],
  "automerge": false,
  "prHourlyLimit": 0,
  "prConcurrentLimit": 5,
  "minimumReleaseAge": "7 days",
  "rebaseWhen": "behind-base-branch",
  "configMigration": true,
  "gitIgnoredAuthors": ["ducktape@basistheory.com"],
  "labels": ["dependencies"],
  "enabledManagers": ["npm", "nuget", "dockerfile", "github-actions", "terraform"],
  "lockFileMaintenance": {
    "enabled": true,
    "schedule": ["before 6am on the first day of the month"]
  },
  "vulnerabilityAlerts": {
    "description": "Security updates bypass minimumReleaseAge per Renovate docs (https://docs.renovatebot.com/key-concepts/minimum-release-age/#what-happens-to-security-updates). No override attempted here.",
    "schedule": [],
    "labels": ["dependencies", "security"]
  },
  "packageRules": [
    {
      "description": "Group all non-major updates into a single PR",
      "matchPackageNames": ["*"],
      "matchUpdateTypes": ["minor", "patch", "digest", "pin"],
      "groupName": "all non-major dependencies",
      "groupSlug": "all-minor-patch"
    },
    {
      "description": "Require Dependency Dashboard approval before opening major update PRs",
      "matchUpdateTypes": ["major"],
      "dependencyDashboardApproval": true
    },
    {
      "description": "Skip digest pinning for aquasecurity/* GitHub Actions. The aquasecurity org has a GitHub IP allow list that blocks Renovate's digest lookups from Modal, causing Renovate to exit non-zero and post warning summaries to #dependency-prs. See README.md.",
      "matchManagers": ["github-actions"],
      "matchPackageNames": ["aquasecurity/**"],
      "pinDigests": false
    }
  ]
}
